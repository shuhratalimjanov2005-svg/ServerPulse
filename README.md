# ServerPulse
1. Установи Python (если еще нет).
2. Установи библиотеки: pip install -r requirements.txt
3. Переименуй файл .env.example в .env
4. Открой .env, вставь туда токен и URL твоего сервера.
5. Запусти агента: python agent.py
6. Если сервер выключился перезапусти агента 
7. Продвинутым пользователям» маленькую заметку: «Чтобы агент запускался сам при старте сервера, используйте systemd (инструкцию можно найти в Google по запросу 'how to run python script as service systemd'
